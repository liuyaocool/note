# 中断

- 数据端口（io设备）
- 中断请求
    - 可编程中断控制器(多个IRQ引脚 对应中断向量号， 每个与io设备相连接)
        - 寄存器 保存中断向量号
        - 拓展： 主片 + 从片(通过IRQ2连接主片，用于扩展)
    - 可编程中断控制器 ----intr线---- CPU
- CPU收到信号后
    - 读取寄存器，拿到号
    - 去中断向量表找对应中断程序
    - 根据中断程序执行相应操作

# 内核态 用户态

